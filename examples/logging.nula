# logging.nula

def something = 5;
log(something);

# when using the logging function, there's no need to dereference references
def ref = &something;
log(ref);

# multiple values spread out on the same line
log(something, ref);